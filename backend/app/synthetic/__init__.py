"""Synthetic invoice/payment dataset generator used to test the matching engine.

See ``app.synthetic.scenarios`` for the canonical dataset definition and the
single entrypoint (``python -m app.synthetic.scenarios``) that writes all
three output files in one deterministic pass.
"""
